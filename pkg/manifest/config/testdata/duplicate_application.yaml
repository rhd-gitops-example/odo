environments:
  - name: dup-application
    pipelines:
      integration:
        template: dev-ci-template
        binding: dev-ci-binding
    apps:
      - name: app-2
      - name: app-2
        services:
          - name: taxi-svc
            source_url: https://github.com/myproject/myservice.git
            webhook:
              secret:
                name: webhook-secret
                namespace: webhook-secret-key
            pipelines:
              integration:
                template: dev-ci-template
                binding: dev-ci-binding
 
