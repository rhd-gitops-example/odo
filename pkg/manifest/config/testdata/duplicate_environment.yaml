environments:
  - name: dup-environment
    pipelines:
      integration:
        template: dev-ci-template
        binding: dev-ci-binding
    apps:
      - name: dup-environment-test
        services:
          - name: taxi-svc
            source_url: https://github.com/myproject/myservice.git
            webhook:
              secret:
                name: webhook-secret
                namespace: webhook-secret-key
            pipelines:
              integration:
                template: dev-ci-template
                binding: dev-ci-binding
  - name: test-cicd
    cicd: true
  - name: stage-env
  - name: stage-env